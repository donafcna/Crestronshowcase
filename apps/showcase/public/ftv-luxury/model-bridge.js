(()=>{'use strict';const api=window.__ftvModel;if(!api)return;const channel='ftv-luxury/v1',isLocalFile=location.protocol==='file:'||location.origin==='null',origin=isLocalFile?'*':location.origin;let last='';
const send=(type,payload={})=>parent.postMessage({channel,project:api.project,type,...payload},origin);
window.__ftvEmit=()=>{const s=api.state(),str=JSON.stringify(s);if(str!==last){last=str;send('selection',s)}};
window.addEventListener('message',e=>{if(e.source!==parent||(!isLocalFile&&e.origin!==location.origin))return;const m=e.data;if(m?.channel!==channel||m.project!==api.project)return;
try{switch(m.type){case 'viewport':if(m.viewport&&['x','y','w','h'].every(k=>Number.isFinite(m.viewport[k]))&&m.viewport.w>0&&m.viewport.h>0)api.viewport?.(m.viewport);break;case 'pick':if(Number.isFinite(m.x)&&Number.isFinite(m.y))api.pick?.(m.x,m.y);break;case 'visibility':window.__ftvPaused=m.visible===false;break;case 'resize':window.dispatchEvent(new Event('resize'));break;case 'select':if(m.id==='all'||m.id==='hall'&&api.project==='boutique-hermes'||api.catalog.some(r=>r.id===m.id))api.select(m.id);break;case 'level':if(m.id==='all'||api.floors.some(r=>String(r.id)===String(m.id)))api.level(m.id);break;case 'overview':api.overview();break;case 'scene':api.scene(m.key);break;case 'lighting':if(m.values&&typeof m.values==='object')api.lighting(m.values,m.scope||'all');break;case 'color':api.color(m.value);break;case 'capture':api.capture();break;case 'hello':send('model-ready',{rooms:api.catalog,floors:api.floors});break;default:return;}window.__ftvEmit();}catch{send('model-error',{message:'Cette commande n’a pas pu être appliquée.'});}});
for(const event of ['pointerdown','keydown','wheel'])document.addEventListener(event,e=>{if(e.isTrusted)send('user-activity')},{capture:true,passive:true});
send('model-ready',{rooms:api.catalog,floors:api.floors});window.__ftvEmit();
})();
